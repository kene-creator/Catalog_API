version: "3.8"
services:
  mongodb:
    image: mongo:latest
    container_name: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - netLecture

volumes:
  mongo_data:
    driver: local
networks:
  netLecture:
    driver: bridge
